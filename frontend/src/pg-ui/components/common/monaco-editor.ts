// @ts-nocheck — vendored from Heimdall-Panel (upstream pg-ui); keep byte-compatible with upstream.
import '@/pg-ui/lib/configure-monaco'

export { default, DiffEditor, loader, useMonaco } from '@monaco-editor/react'
