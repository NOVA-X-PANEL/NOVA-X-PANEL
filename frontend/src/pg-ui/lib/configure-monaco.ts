// @ts-nocheck — vendored from Heimdall-Panel (upstream pg-ui); keep byte-compatible with upstream.
// Monaco is loaded lazily by editor components. This module intentionally centralizes
// optional runtime configuration and is safe as a side-effect import in tests/builds.
export {};
